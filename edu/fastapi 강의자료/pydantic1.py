"""
API 뿐만 아니라 우리가 흔히 웹페이지를 들어가면 `https://example.com?no=1&page=32`등과 같은 주소 형식을 많이 보게 됩니다. 호스트 주소 뒤 `?` 뒤에 오는 변수들을 쿼리 매개변수(Query parameters)라 부릅니다. 
각 매개변수는 `&` 기호로 구분되고 `key=value` 와 같이 키/값 쌍으로 정의합니다.
"""